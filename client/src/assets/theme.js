import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        // Navbar
        primary: {
            main: "#0d47a1",
            dark: "skyblue"
        },

        // Main
        error:{
            main: "#1565c0"
        }
        ,
        // Use for Icon
        code: {
            main: "#1565c0"
        }
    }
})